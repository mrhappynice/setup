Setup Termux:

1. **Update and Upgrade**:
   - Open Termux and update package lists:
     ```bash
     pkg update -y && pkg upgrade -y
     ```
2. **Install wget**:
   - Open Termux and update package lists:
     ```bash
     pkg install wget
     ```

